{% extends 'base.html.twig' %}

{% block title %}Add Product{% endblock %}

{% block body %}

<div class="row my-5">
    <div class="col-md-6 mx-auto">
        <div class="card">
            <div class="card-header">
                <span>Add Product</span>
                <a href="{{ path('app_product') }}" class="btn btn-sm btn-secondary float-end">Back</a>
            </div>
            <div class="card-body">
                {{ form(form) }}
            </div>
        </div>
    </div>
</div>

{% endblock %}